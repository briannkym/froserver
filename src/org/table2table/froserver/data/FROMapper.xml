<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.table2table.froserver.data.FROMapper">
  

  <insert id="addPickup">
      INSERT INTO pickuppounds (site, date, category, pounds)    
      VALUES (#{site}, #{date}, #{category}, #{pounds});
  </insert>
  
  <insert id="addDropoff">
      INSERT INTO dropoffpounds (site, date, category, pounds)
      VALUES (#{site}, #{date}, #{category}, #{pounds})
  </insert>
  
  
</mapper>